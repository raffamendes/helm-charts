apiVersion: v1
kind: Service
metadata:
  name: {{ include "expense-netcore-api.fullname" . }}
  labels:
    {{- include "expense-netcore-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "expense-netcore-api.selectorLabels" . | nindent 4 }}
